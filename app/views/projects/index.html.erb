<div class="mt-32">
  <h1 class="text-4xl font-bold text-white text-center">Check out some projects build by our mentors</h1>
  <p class="text-white text-center w-[60%] mx-auto mt-5 text-sm">
    Discover the magic of collaboration as you explore a medley of projects, crafted by our skilled mentors, united by their vision for extraordinary creations.
  </p>
</div>
<div class="flex justify-center items-center my-32 gap-x-5">
  <% @projects.each_with_index do |project,index|  %>
    <div class="w-full max-w-sm bg-white border border-gray-200 rounded-lg shadow dark:bg-gray-800 dark:border-gray-700">
      <a href="<%= project_path(project) %>" class="text-white text-3xl flex justify-center mb-10 border-b border-slate-300">
        Project-<%= index+1 %>
      </a>
      <div class="px-5 pb-5">
        <h5 class="text-xl font-semibold tracking-tight text-gray-900 dark:text-white"><%= project.title %></h5>
        <p class="mt-2 text-sm text-gray-500 dark:text-gray-400"><%= (project.description).truncate(100) %></p>
        <div class="flex items-center justify-between">
          <span class="text-3xl font-bold text-gray-900 dark:text-white"><%= project.bid.amount if project.bid %></span>
          <a href="<%= project_path(project) %>" class="text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800 mt-2">Show</a>
        </div>
      </div>
    </div>
  <% end %>
</div>
<% if current_user && current_user.admin? %>
  <%= button_to "Delete Expired Bids",delete_all_expired_bids_path,remote: true,method: :post,class:"bg-red-600 hover:bg-red-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:ring focus:border-blue-300" %>
<% end%>
